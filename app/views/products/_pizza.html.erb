<div class="menu-item pizza">
  <%= image_tag pizza.photo_url || "pizza.png" %>
  <%= content_tag :h3, pizza.name %>
  <%= content_tag :p, pizza.description %>
  <% if can?(:manage, pizza) %>
      <%= link_to "Изменить", edit_product_path(pizza) %> / <%= link_to 'Удалить', pizza, method: :delete, data: {confirm: 'Удаляем?'} %>
  <% end %>

  <div class="order-button">
    Заказать за <span class="price" onclick="addProduct('<%= add_to_cart_path(pizza.id, 1) %>');"><%= pizza.price_classic %></span> / <span class="price price-big" onclick="addProduct('<%= add_to_cart_path(pizza.id, 2) %>');"><%= pizza.price_big %> &#8399;</span>
  </div>
</div>